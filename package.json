{
  "name": "virtual-devops-copilot-poc",
  "private": true,
  "scripts": {
    "install": "cd app && npm install",
    "verify-install": "ls -la app/node_modules/.bin",
    "test": "cd app && npm test",
    "start": "cd app && node index.js"
  }
}
